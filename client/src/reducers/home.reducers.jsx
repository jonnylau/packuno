
const packingListRed = (state, action) => {
  if (action.type === 'ADD_ITEM') {


  } else if (action.type === 'TOGGLE_PACKED') {

  } else if (action.type === 'SET_CAT_FILTER') {

  } else {
    return state;
    
  }
};